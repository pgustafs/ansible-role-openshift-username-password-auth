galaxy_info:
  author: Peter Gustafsson
  description: A role to authenticate to an OpenShift clusters with username and password to retrive an authentication token that can then be used with k8s* modules to interact with the kubernetes API and then logs out (revokes the authentication token).
  company: Red Hat

  license: license (GPLv2, CC-BY, etc)

  min_ansible_version: 2.9

  platforms:
  - name: EL
    versions:
    - all

  categories:
  - all

  galaxy_tags:
  - openshift
  - kubernetes

dependencies: []
